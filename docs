Prerequis :

docker run -ti -p 4000:4000 docs/docker.github.io:latest


Install docker-machine and configure with virutal machine
	docker-machine -> on mac os or windows to interact with docker
	docker-machine ls -> list avalaible machine
	docker-machine create -> create a machine
		--driver : select appropriate driver
	docker-machine env -> get the env command

1)
01 	docker-machine create --driver virtualbox Char
02 	docker-machine ip Char
03 	docker-machine env Char
	eval "$(docker-machine env Char)"
04	docker pull hello-world
05	docker run hello-world

	docker run
	-d (detach) run container in background
	-p (publish list) puclish a container's port to the host
	--name assign a name to the container

06	docker run -d -p 5000:80 --name overlord --restart always nginx
07	docker inspect overlord | grep IPAddress
08	docker run -it --rm alpine
09	docker run -it --rm debian
		apt-get update -y && apt-get upgrade -y && apt-get install -y gcc && apt-get install -y git

10	docker volume create hatchery
11	docker volume ls
12
	-d (detach)
	-v (bind mount a volume) hatchery:/var/lib/mysql (default folder for msql in container)
	--restart always
	-e MYSQL_ROOT_PASSWORD=Kerrigan
	   MYSQL_DATABASE=zerglings
	--name spawnin-pool
docker run -d -v hatchery:/var/lib/mysql \
--restart always \
-e MYSQL_ROOT_PASSWORD=kerrigan \
-e MYSQL_DATABASE=zerglings \
--name spawning-pool\
 mysql

13	docker exec -it spawning-pool bash -c 'printenv'

14	docker run -d --name lair --link spawning-pool:mysql -p 8080:80 -e WORDPRESS_DB=mysql -e WORDPRESS_DB_NAME=zergling -e WORDPRESS_DB_PASSWORD=kerrigan wordpress
